// declare routes

export const book = Object.freeze({
    board:               '/',
    registration:        '/registration',
    login:               '/login',
    exception:           '/exception',
    exceptionStatusCode: '/exception/:statusCode',
});
