// declare routes

export const book = Object.freeze({
    board:               '/',
    registration:        '/registration',
    login:               '/login',
    register:            '/register',
    exception:           '/exception',
    exceptionStatusCode: '/exception/:statusCode',
});
