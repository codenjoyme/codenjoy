version: '3.3'

services:
   codenjoy_db:
     ports:
       - "${POSTGRES_PORT}:5432"

#B#   codenjoy_balancer_frontend:
#B#     ports:
#B#       - "${BALANCER_FRONTEND_PORT}:80"

#B#   codenjoy_balancer:
#B#     ports:
#B#       - "${BALANCER_PORT}:8080"

   codenjoy_server:
     ports:
       - "${CODENJOY_PORT}:8080"

